/* previous-selected-plan */

/* const previousPlan = document.getElementById('previous-selected-plan') as HTMLInputElement;
const customerPlan = localStorage.getItem('customer_plan');
if (previousPlan && customerPlan) {
    previousPlan.value = customerPlan;
} */